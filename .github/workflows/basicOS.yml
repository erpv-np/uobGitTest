name: My Workflow File
on:
  push:
    branches: [main]

Jobs: 
  builds:
  # create vm with ubuntu OS
    runs-on: Ubuntu- 16.04

    steps:
    - name: greeting steps
      run:
        echo "Hello World"
    
